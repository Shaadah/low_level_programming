#include <stdio.h>
/**
 * main - A progrma that prints using the function printf followed by a new line
 * Return: 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
